# downloaded for hyprland:
# hyprland https://wiki.hyprland.org/hyprland-wiki/
# hyprpaper uninstalling
# swaybg
# kitty
# waybar uninstalling
# eww-wayland https://elkowar.github.io/eww/configuration.html
# wofi
# nemo
# dunst
# xsettingsd https://github.com/derat/xsettingsd/wiki

# autogenerated = 1 # remove this line to remove the warning

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor = eDP-1, 1920x1200@60, auto, 0.75

# default order: (laptop display -> HDMI -> back usb-c)
# front usb not tested, assumed after back given name
# Names:
# eDP-1     : laptop display
# HDMI-A-1  : HDMI port
# DP-1      : back usb-c
# DP-2      : front usb-c
monitor = , preferred, auto, 0.75 # generic monitor


# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute once on launch
# Admin
$dbus_env = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
$key_rng = gnome-keyring-daemon --start --components=secrets
$su_auth = /usr/lib/polkit-kde-authentication-agent-1

# Themeing
$su_cursor = hyprctl setcursor Bibata-Modern-Classic 10
$gnome_buttons = gsettings set org.gnome.desktop.wm.preferences button-layout :
$su_bg = hyprpaper -c /home/penguin/.config/hypr/hyprpaper.conf

# Hardware
$su_nobuzz = pactl unload-module module-suspend-on-idle

# Scripts
$su_notifs = /usr/bin/dunst -conf ~/.config/dunst/dunstrc/dunstrc 
$battery_script = /home/penguin/.config/hypr/battery_notification.sh
$su_eww = /home/penguin/.config/eww/eww/target/release/eww daemon
$su_bar = /home/penguin/.config/eww/eww/target/release/eww open status-bar
$app_fix = /home/penguin/.config/hypr/app_startup_fix.sh


exec-once = $dbus_env & $key_rng & $su_auth & $su_cursor & $gnome_buttons & $su_bg & $su_nobuzz & $su_notifs & $battery_script & $su_eww & $su_bar & $app_fix


# Emergency keybinds in main conf file
# These WILL NOT work in submaps
# keep a kitty all the time
# and in every submap
bind = SUPER ALT SHIFT, M , exit,
bind = SUPER          , Q , exec, kitty
bind =       ALT      , f4, killactive,

# keybindings cheat sheet script (WIP)
bind = SUPER ALT, 1, exec, python3 /home/penguin/.config/hypr/keybinds/KeybindingHintWindow.py


# Colors
source = ~/.config/hypr/colors.conf

# keybinds
source = ~/.config/hypr/keybinds/keybindings.conf

# damage_tracking = full

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us
    # kb_variant =
    # kb_model =
    # kb_options =
    # kb_rules =

    follow_mouse = 2
    accel_profile = flat
    scroll_method = 2fg

    touchpad {
        middle_button_emulation = false
        clickfinger_behavior = true

        tap-to-click = true
        drag_lock = true

        disable_while_typing = true

        natural_scroll = true
        scroll_factor = 0.5
    }

    sensitivity = 1.0 # [-1.0, 1.0], 0 means no modification.
}

binds {
    # allow_workspace_cycles = true
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = -2
    gaps_out = -2
    border_size = 2

    col.active_border = $act_bor
    col.inactive_border = $inact_bor

    col.group_border_active = $act_gro_bor
    col.group_border = $gro_bor

    layout = dwindle
    
    no_cursor_warps = false
    cursor_inactive_timeout = 30
    
    no_border_on_floating = false

    # Authenticator rules
    windowrule = float,^(org.kde.polkit-kde-authentication-agent-1)$
    windowrule = move cursor -50% -50%,^(org.kde.polkit-kde-authentication-agent-1)$
    windowrule = rounding 10,^(org.kde.polkit-kde-authentication-agent-1)$
    windowrule = size 500 300,^(org.kde.polkit-kde-authentication-agent-1)$
    windowrule = dimaround,^(org.kde.polkit-kde-authentication-agent-1)$
    windowrule = stayfocused,^(org.kde.polkit-kde-authentication-agent-1)$

    # Kitty Window rules
    windowrule = float,^(kitty)$
    windowrule = move cursor -50% -50%,^(kitty)$ # moves kitty to the center of the cursor
    windowrule = opacity 1.0 override 0.4 override,^(kitty)$ # set opacity to 1.0 active and 0.5 inactive for kitty
    windowrule = rounding 10,^(kitty)$ # set rounding to 10 for kitty
    windowrule = size 800 500,^(kitty)$ # set rounding to 10 for kitty

    # Calculator Window rules
    windowrule = float,^(qalculate-gtk)$
    windowrule = move cursor -50% -50%,^(qalculate-gtk)$ # moves kitty to the center of the cursor
    windowrule = opacity 1.0 override 0.4 override,^(qalculate-gtk)$ # set opacity to 1.0 active and 0.5 inactive for kitty
    windowrule = rounding 10,^(qalculate-gtk)$ # set rounding to 10 for kitty
    windowrule = size 800 500,^(qalculate-gtk)$ # set rounding to 10 for kitty
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
	
	multisample_edges = true
    rounding = 0
    
    active_opacity = 1.0
    fullscreen_opacity = 1.0
    inactive_opacity = 1.0

    drop_shadow = false
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = $shadow

    dim_inactive = false
    dim_strength = 0.1

    blur {
        enabled = false
        size = 1
        passes = 1
        ignore_opacity = false
        new_optimizations = true
        xray = true
    }
}

animations {
    enabled = false

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = true # master switch for pseudotiling.
    
    force_split = 0
    preserve_split = true
    smart_split = false

    smart_resizing = true
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_is_master = true
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = true
    workspace_swipe_fingers = 4
    workspace_swipe_distance = 300
    workspace_swipe_invert = true
    workspace_swipe_numbered = false
}

xwayland {
	use_nearest_neighbor = false
	force_zero_scaling = true
}

misc {
    disable_hyprland_logo = true
    disable_splash_rendering = true
    force_hypr_chan = false
    # force_default_wallpaper = 0

    enable_swallow = false

    vfr = true
    vrr = 1
    
    cursor_zoom_factor = 1.0

    render_ahead_of_time = false
    render_ahead_safezone = 4
}

debug {
    damage_tracking = 2

    damage_blink = false

    overlay = false
}
